# ansible-galaxy collection install -r requirements.yml
# Nécessite ansible-core >= 2.17 (pip install --user --upgrade ansible).
collections:
  - name: community.docker
    version: ">=3.10.0"
  - name: community.general # module ufw
    version: ">=9.0.0,<13.0.0" # 13+ exige ansible-core 2.18 (Python 3.11+)
